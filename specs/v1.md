new features (new major version):

- add a "timeline" view to the app that shows all requests in a organized way
  - also add, similar to chrome dev tools a timeline slider to allow filtering the requests
- limit the max requests to be stored in memory to avoid memory leaks (use a generous limit)
- add a "clear history" button
- add a "add marker" to mark time moment, and show it in the UI so is is clear the before/after
- fira code font is not loading? if not fix it
- today only done requests are tracked, lets track also pending requests
- add a new api statistics tab with useful info/charts about the api calls
  - focus on tracking performance issues like:
    - slow requests
    - heavy payload requests
    - duplicated requests made in short periods of time that have same request payload/response
- add a new request caller api tab (similar to postman), the caller must be configured by the lib consumer so the current data fetching mechanisms can be used, maybe even more than one caller may be configured, check /Users/lucasoliveirasantos/Github/jestor/web-app/ for a usage reference, but basically it should abstract all unecessary details
  - in the requests panel a new option to "modify request" should be presented to copy it to the caller tab
- use a new json viewer similar to the one used in /Users/lucasoliveirasantos/Github/agent-eval and also add a json searcher similar to the one form agent eval too
- allow to open the devtools in a new window
- add a copy history option as json or save to a .json file, this should allow to choose what responses will be copied, and also offer a privacy mode that instead of copying the full data response, copies a simplified version, like /Users/lucasoliveirasantos/Github/jestor/web-app-2/utils-lib/removeSensitiveData.ts does
- add in the requests detail panel a option or button to copy it as curl
